# Wildcards

```bash
.       # Everything in this directory
*       # Every file / directory in current directory
**      # Every file / directory in all directories inside current (recursive)
?       # Any single character
**      # Every file + directory
foo*    # Everything starting with "foo"
*.txt   # Everything ending with ".txt"
[]      # Range of values
{}      # List of values
```